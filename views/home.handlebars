<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css"
        integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">
</head>

<body>
    <div>
        {{#if users}}
        <table style="margin-left: 20px; width: 95%" class="table table-sm table-hover">
            <thead>
                <th>Id</th>
                <th>First Name</th>
                <th>Last Name</th>
            </thead>
            <tbody class="table-hover">
                {{#each users}}
                <tr>
                    <td>{{this.id}}</td>
                    <td>{{this.first_name}}</td>
                    <td>{{this.last_name}}</td>
                    <td>
                        <a href="/user/edit/{{this.id}}" class="btn btn-primary">Edit</a>
                        <a href="/user/delete/{{this.id}}" class="btn btn-warning">Delete</a>
                    </td>
                </tr>
                {{/each}}
            </tbody>
        </table>
        {{else}}
        <h1>There are no users to display, please add users first</h1>
        {{/if}}
        {{!-- this button will send a GET request to the route user/add, from which we will render the page add-user.hanldebars --}}
        <div style="margin-left: 20px; witdth: 95%">
            <a href="user/add" class="btn btn-info">Add new user</a>
        </div>
        <hr />


    </div>


</body>

</html>